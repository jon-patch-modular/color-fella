# Normalize EOL for all files that Git considers text files.
* text=auto eol=lf
Recursos/Audio/*.zip filter=lfs diff=lfs merge=lfs -text
Recursos/Audio/_projectes/*.zip filter=lfs diff=lfs merge=lfs -text
